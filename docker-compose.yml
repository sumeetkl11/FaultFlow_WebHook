services:
  postgres:
    image: postgres:16-alpine
    container_name: faultflow-postgres
    restart: always
    environment:
      POSTGRES_USER: faultflow_admin
      POSTGRES_PASSWORD: local_secret_password
      POSTGRES_DB: faultflow_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U faultflow_admin -d faultflow_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: faultflow-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
